<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lz.wms">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".APP"
        android:allowBackup="true"
        android:appComponentFactory=""
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:appComponentFactory">
        <activity android:name=".InitActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.menu.MenuActivity" />
        <activity android:name=".ui.inbound.InboundActivity" />
        <activity android:name=".ui.outbound.OutboundActivity" />
        <activity android:name=".ui.breakage.BreakageActivity" />
        <activity android:name=".ui.movement.MovementActivity" />
        <activity android:name=".ui.check.CheckActivity" />
        <activity android:name=".ui.scheduling.SchedulingActivity" />
        <activity android:name=".ui.setting.SettingActivity" />
        <activity android:name=".ui.inbound.AddInboundActivity"></activity>
    </application>

</manifest>